# ==================================================
# TK-Ops-Pro V2.0 配置文件
# ==================================================

# 基础设置
THEME_MODE=dark
# 下载目录：未配置时默认使用 AssetLibrary/Downloads
# DOWNLOAD_DIR=D:\TK_Ops\Downloads

# 数据源: EchoTik (V2.0 主要通过 Excel 导入，API 可选)
# 获取地址: https://echotik.live/platform/api-keys
ECHOTIK_API_KEY=your_key_here
ECHOTIK_API_SECRET=your_secret_here

# ==================================================
# V2.0 利润核算默认参数 (Profit Calculator)
# ==================================================
EXCHANGE_RATE=7.25             # 美元兑人民币汇率
SHIPPING_COST_PER_KG=12.0      # 头程运费 ($/kg)
PLATFORM_COMMISSION=0.05       # 平台佣金比例 (5%)
FIXED_FEE=0.3                  # 单笔固定费用 ($)

# ==================================================
# V2.0 AI 服务 (DeepSeek 脚本生成 + Edge-TTS 语音)
# ==================================================
AI_PROVIDER=deepseek           # openai | deepseek | compatible
AI_BASE_URL=https://api.deepseek.com  # 兼容 OpenAI 的 Base URL
AI_API_KEY=sk-xxxxxxxx         # AI API Key（DeepSeek/OpenAI 兼容）
AI_MODEL=deepseek-chat         # 模型名称（可在系统设置中拉取模型列表）

# AI 角色（System Prompt，可选）：用于“AI 爆款文案助手 + AI 二创工厂”
# AI_SYSTEM_PROMPT=你是一名强转化的 TikTok 带货主播，输出更直接、更有号召力。

# 面板级“自定义角色提示词”（会在面板输入框中自动保存，下次打开自动回填）
AI_COPYWRITER_ROLE_PROMPT=
AI_FACTORY_ROLE_PROMPT=

# TTS 语音合成
# 建议：生产优先用 volcengine（豆包/火山）；edge-tts 免费但可能遇到 403 风控
TTS_PROVIDER=edge-tts          # edge-tts (免费) | volcengine (豆包/火山)
TTS_FALLBACK_PROVIDER=         # 可选：edge-tts | volcengine | 留空=不启用

# Edge-TTS 语音（仅当 TTS_PROVIDER/备用=edge-tts 时生效）
TTS_VOICE=en-US-AvaNeural      # 美式英语女声 (或 en-US-GuyNeural 男声)
TTS_SPEED=1.1                  # 语速倍率（1.0=正常）

# 豆包/火山 TTS（当前实现为 Token 模式：OpenSpeech 风格接口）
# 控制台会提供 3 个关键参数：APP ID / Access Token / Secret Key
VOLC_TTS_ENDPOINT=https://openspeech.bytedance.com/api/v1/tts
VOLC_TTS_APPID=
VOLC_TTS_ACCESS_TOKEN=
VOLC_TTS_SECRET_KEY=

# 兼容旧键（可留空）：老版本会读取 VOLC_TTS_TOKEN
VOLC_TTS_TOKEN=
VOLC_TTS_CLUSTER=volcano_tts
VOLC_TTS_VOICE_TYPE=
VOLC_TTS_ENCODING=mp3

# ==================================================
# V2.0 局域网传输服务
# ==================================================
LAN_PORT=8000                  # HTTP 服务端口
LAN_ENABLED=false              # 是否默认启动局域网服务

# 选品阈值默认值
GROWTH_RATE_THRESHOLD=50
MAX_REVIEWS=1000
PRICE_MIN=10
PRICE_MAX=100

# 视频处理
VIDEO_DEEP_REMIX_ENABLED=0
VIDEO_REMIX_MICRO_ZOOM=1
VIDEO_REMIX_ADD_NOISE=0
VIDEO_REMIX_STRIP_METADATA=1
